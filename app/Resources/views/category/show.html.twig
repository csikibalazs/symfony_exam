{% extends 'base.html.twig' %}

{% block body %}
    <h1>Category</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Category</th>
                <td>{{ category.category }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ category.slug }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('innonic_job_category_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('innonic_job_category_edit', { 'id': category.id }) }}">edit</a>
        </li>
    </ul>
{% endblock %}
